import { JoinWordsPipe } from './join-words.pipe';

describe('JoinWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
